import React from 'react';
import JobApplicationForm from './Components/Visitors/Visitor'; // Make sure the path is correct
import 'bootstrap/dist/css/bootstrap.min.css'; // Import Bootstrap CSS if not already done

function App() {
  return ( // Add return statement here
    <div className="App">
      <JobApplicationForm />
    </div>
  );
}

export default App;
